[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "flexprice-protos"
version = "0.1.0"
dependencies = [
    "grpcio>=1.56.0",
    "protobuf>=4.23.4",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["flexprice_python_pb*"]
